# Example Environment Variables for Frontend Development

# Mock Data Configuration
# Set to 'true' to enable mock pod data for development and testing
# Set to 'false' or leave unset for production behavior (uses real API)
# WARNING: NEVER set this to 'true' in production deployments!
REACT_APP_USE_MOCK_DATA=false

# API Configuration
# Base URL for API calls - defaults to '/api' (works with nginx proxy)
# REACT_APP_API_BASE_URL=/api

# Environment Detection
# Custom environment override for production detection
# Used in addition to NODE_ENV for mock data safety
# REACT_APP_ENV=production

# Development Server Configuration
# Port for the development server - defaults to 3000
# PORT=3000

# Build Configuration
# Generate source maps for production builds
# GENERATE_SOURCEMAP=false

# Note: 
# - Copy this file to .env and modify as needed for local development
# - Example: cp .env.example .env
# - Variables prefixed with REACT_APP_ are available in the browser
# - Other variables are only available during build time
